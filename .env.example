# Copy to .env and fill in for real-hardware runs:
#   cp .env.example .env
# docker compose reads .env automatically; it's only needed with docker-compose.real.yml.

ROBOT_IP=192.168.1.10
KINEMATICS_PARAMS_FILE=/root/ros2_ws/calibration/ur3e_calibration.yaml
